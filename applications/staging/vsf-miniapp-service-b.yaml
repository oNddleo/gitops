apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vsf-miniapp-service-b-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: staging
    app: vsf-miniapp
    service: service-b
    language: nodejs
spec:
  project: default

  source:
    repoURL: https://github.com/oNddleo/gitops.git
    targetRevision: HEAD
    path: charts/vsf-miniapp
    helm:
      valueFiles:
        - ci/service-b-staging.yaml
      releaseName: service-b

  destination:
    server: https://kubernetes.default.svc
    namespace: staging

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - Validate=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas

  info:
    - name: 'Environment'
      value: 'Staging'
    - name: 'Service'
      value: 'Service B (Node.js Express)'
    - name: 'Team'
      value: 'Platform Team'
