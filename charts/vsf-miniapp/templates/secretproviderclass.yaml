{{- if .Values.secretsManager.enabled }}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: {{ include "vsf-miniapp.fullname" . }}-secrets
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vsf-miniapp.labels" . | nindent 4 }}
spec:
  provider: aws
  parameters:
    region: {{ .Values.secretsManager.region }}
    {{- with .Values.secretsManager.objects }}
    objects: |
      {{- toYaml . | nindent 6 }}
    {{- end }}

  {{- if .Values.secretsManager.syncToKubernetesSecret }}
  # Sync secrets to Kubernetes Secret for use in environment variables
  secretObjects:
    - secretName: {{ include "vsf-miniapp.fullname" . }}-secrets
      type: Opaque
      labels:
        {{- include "vsf-miniapp.labels" . | nindent 8 }}
      annotations:
        reloader.stakater.com/match: "true"
      data:
        {{- range .Values.secretsManager.secretData }}
        - objectName: {{ .objectName }}
          key: {{ .key }}
        {{- end }}
  {{- end }}
{{- end }}
